
<div class="row" style="margin-top: 100px; margin-bottom: 100px;">
  <% if(!displayName) {%> 
    <h1 style="color:black">Đăng nhập mới được Bet mấy cha</h1>
     <% } %>
    <div class="col">
    <% if(displayName) {%> 
      <a href="/contact-list/add" id="addButton" class="btn btn-primary mb-1" ><i class="fas fa-plus-circle"></i> Thêm Tỉ Số</a>
    <% } %>
        <!--Table goes here-->
         <table class="table table-striped table-bordered table-hover" style="opacity:0.75;background-color:#FFFFE0;border-radius: 5px; box-shadow: 3px 3px 3px #808080; font-family:Arial, Helvetica, sans-serif, Garamond, Serif;color:black;font-style: italic; ">
            <thead class="table-dark">
              <tr>
                <th scope="col">#</th>
                <th scope="col">Tên </th>
                <th scope="col">Hôm nay bạn nghĩ gì ^^</th>
                <th scope="col">Tỉ Số</th>
                <% if(displayName) {%> 
                <th></th>
                <th></th>
                <% } %>
              </tr>
            </thead>
            <tbody>

              <!-- Repeatable Row Template -->

              <% for(let count = 0; count < contacts.length; count++) { %>
              <tr>
                <th scope="row"><%= count+ 1 %></th>
                <td><%= contacts[count].FullName %></td>
                <td><%= contacts[count].EmailAddress %></td>
                <td><%= contacts[count].ContactNumber %></td>
                 <% if(displayName) {%> 
                  <td class="text-center"><a href="/contact-list/edit/<%= contacts[count]._id %>" class="btn btn-primary btn-sm edit"><i class="fas fa-edit fa-sm"></i> Sửa cũng ko ăn được đâu</a></td>
                  <td class="text-center"><a href="/contact-list/delete/<%= contacts[count]._id %>" class="btn btn-danger btn-sm delete"><i class="fas fa-trash-alt fa-sm"></i> Từ bỏ cuộc chơi</a></td>
                 <% } %>
              </tr>

              <% } %>
            </tbody>
          </table>
    </div>
</div>